version: 1.0
runtime: python311
build:
  commands:
    build:
      - echo "Installing dependencies"
      - pip install -r requirements.txt
      - echo "Build completed"
run:
  runtime-version: 3.11
  command: cd backend/interactive1 && python app.py
  network:
    port: 8080
  env:
    - name: PORT
      value: 8080
    - name: AWS_DEPLOYMENT
      value: truee: python3
build:
  commands:
    build:
      - echo "Installing dependencies"
      - pip install -r requirements.txt
      - echo "Build completed"
run:
  runtime-version: 3.11
  command: cd backend/interactiveApp && python app.py
  network:
    port: 8080
  env:
    - name: PORT
      value: 8080
    - name: AWS_DEPLOYMENT
      value: true